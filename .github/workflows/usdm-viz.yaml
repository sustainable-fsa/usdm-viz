# Canonical archive-update workflow (sustainable-fsa S3 migration).
name: update
run-name: Update the usdm-viz archive → S3

on:
  schedule:
    - cron: '0 18 * * 4'
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['**.R', '.github/workflows/**']

permissions:
  id-token: write
  contents: read

concurrency:
  group: s3-archive-${{ github.repository }}
  cancel-in-progress: false

defaults:
  run:
    shell: bash -l {0}

jobs:
  update:
    runs-on: macos-latest
    timeout-minutes: 350
    env:
      S3_BUCKET: sustainable-fsa
      S3_PREFIX: usdm-viz
      AWS_REGION: us-west-2
      CLOUDFRONT_DISTRIBUTION_ID: E1BNL6ONVN84RI
      CLOUDFRONT_BASE: https://data.sustainable-fsa.com
    steps:
      - uses: mt-climate-office/actions/setup-geospatial@v1

      - name: Configure AWS credentials (GitHub OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::202533506375:role/sustainable-fsa-github-actions
          aws-region: us-west-2
          role-duration-seconds: 14400

      - name: Verify AWS identity
        run: aws sts get-caller-identity

      - name: Install packages not in the geospatial environment
        run: |
          Rscript -e 'pkgs <- setdiff(c("png","ragg","cols4all","av","archive","httr2"),
                                      rownames(installed.packages()));
                      if (length(pkgs)) install.packages(pkgs, repos = "https://cloud.r-project.org")'

      - name: Update archive
        run: Rscript usdm-viz.R
